WYVERN PROLOGUE VISIBLE-TEXT PATCH

Replace BOTH files:
1. sample.html
2. assets/styles.css

Then deploy and perform a hard refresh with Ctrl+F5.

This patch includes the full prologue and a high-specificity light parchment override
so older dark manuscript styles cannot make the text unreadable.
